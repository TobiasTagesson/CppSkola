#ifndef __OBJECT6_H
#define __OBJECT6_H

#include <string>
#include <vector>

    class Dog {

    public:
        std::string name;
        std::string breed;
        int age;
        float weight;

        //Dog(std::string name, std::string breed, int age, float weight);
   /*      Dog registerDog();
        void searchDogs(std::vector<Dog> &dogs);
        void printDogsWithTailLengthLongerThanUserInput(const float &maxTailLength, const std::vector<Dog> &dogs);
        float calculateTailLength(const Dog &dog);
        void removeDog(std::vector<Dog> dogs);
        void mainMenu();
        int menuSelection();
        void printMenu(); */

    };


void run_object6();

#endif // __OBJECT6_H